<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 聚焦状态：渐变底色 + 红色边框 -->
    <item android:state_focused="true"
        android:drawable="@drawable/bg_gradient_focus_red_border"/>

    <!-- 默认未聚焦：仅渐变底色 -->
    <item
        android:drawable="@drawable/bg_gradient_normal"/>
</selector>